<!DOCTYPE html>
<html>
<head>
	<title>報名系統</title>
	<link rel="stylesheet" type="text/css" href="../main.css">
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>
<body>
	<% if(message.length > 0) {%>
		<div class="alert alert-success">
			<%= message %>
		</div>
	<% }%>
	<h1>Hi! <%= userName %></h1>
	<nav>
		<ul>
			<li style="font-size: 0.99em"><a href="/">活動列表</a></li>
			<li style="font-weight: bold; font-style: italic; font-size: 1.05em;"><a href="/admin">活動管理</a></li>
			<li style="font-size: 0.99em"><a href="/mine">我的活動</a></li>
			<li style="font-size: 0.99em"><a href="/new">發起活動</a></li>
			<li style="font-size: 0.99em"><a href="/personal">個人資料</a></li>
			<% if(logged) {%>
				<li style="font-size: 0.99em"><a href="/logout">登出</a></li>
			<% } else {%>
				<li style="font-size: 0.99em"><a href="/login">登入</a></li>
			<% } %>
		</ul>
	</nav>
	<hr>
	<div id="container">
		<% if(num == 0) { %>
		<h2>目前無管理活動</h2>
		<% } %>
		<% if(num > 0) { %>
			<table class="table table-hover">
				<thead>
					<tr>
						<th scope="col">活動名稱</th><th scope="col">日期</th><th scope="col">人數上限</th><th scope="col">報名人數</th><th scope="col">主辦人</th><th scope="col"> </th>
					</tr>
				</thead>
				<tbody>
					<% for(let i = 0; i < num; i++) { %>
						<tr>
							<td><%= text[i]['name'] %></td><td><%= text[i]['date'] %></td><td><%= text[i]['limits'] %></td><td><%= text[i]['number'] %></td><td><%= text[i]['admin'] %></td>
							<td><a href="/activity/<%= text[i]['eid'] %>/edit">編輯</a></td>
						</tr>
					<% } %>
				</tbody>
			</table>
		<% } %>
	</div>
</body>
</html>